# STATIC TEMPLATE — copy verbatim, substitute operational-excellence.
# Placeholder only. No real secrets are ever committed by the skill — the
# hardening team populates these (e.g. via sealed-secrets or the cluster's
# secret manager). SSO/auth credentials belong here, behind the dev seam.
apiVersion: v1
kind: Secret
metadata:
  name: operational-excellence-secrets
type: Opaque
stringData:
  PLACEHOLDER: "set-by-hardening-team"
